domain: mailserver.lokole.ca

version:
  imageRegistry: ascoderu
  dockerTag: latest

server:
  serverWorkers: 2

worker:
  queueWorkers: 1

logging:
  level: INFO

nginx-ingress:
  rbac:
    create: false
    createRole: false
    createClusterRole: false
  controller:
    replicaCount: 3

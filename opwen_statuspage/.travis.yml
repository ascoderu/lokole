language: node_js

node_js:
  - 12

cache: npm

install: npm install
script: npm run ci

before_deploy:
  - npm run build

deploy:
  provider: pages
  skip-cleanup: true
  github-token: $GITHUB_TOKEN
  local-dir: build
  on:
    branch: master
